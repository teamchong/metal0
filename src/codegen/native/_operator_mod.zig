/// Python _operator module - C accelerator for operator (internal)
/// Re-exports operator module - same API
/// MIGRATED TO ZIGBUILDER (re-export only)
pub const Funcs = @import("operator_mod.zig").Funcs;
