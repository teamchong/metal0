[project]
name = "pytest_compat"
version = "0.1.0"
description = "Minimal pytest compatibility for Zyth"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
