//! Python stdlib module stub
//! Not needed: Native Zig compression via vendor/libdeflate/
const std = @import("std");

pub fn __not_applicable__() void {
    // Stub - see module header for why this isn't needed
}
