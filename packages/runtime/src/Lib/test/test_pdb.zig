//! Python stdlib module stub
//! Not needed: Python debugger - use native debuggers (lldb/gdb) on AOT binaries
const std = @import("std");

pub fn __stub__() void {
    // Stub - see module header for why this isn't needed
}
